'use strict';

describe('wuptime.overview module', function() {

  beforeEach(module('wuptime.overview'));

  describe('overview controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var overviewCtrl = $controller('serviceList');
      expect(serviceList).toBeDefined();
    }));

  });
});
